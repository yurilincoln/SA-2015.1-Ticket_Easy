package br.senai.sc.Dao;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import br.senai.sc.Model.Pacote;

public class PacoteDao { /*
	
	public static EntityManagerFactory entityManagerFactory;


		EntityManagerFactory entityManagerFactory = Persistence.createEntityManagerFactory("TicketEasy");
		EntityManager entityManager = entityManagerFactory.createEntityManager();
	}

	private static void cadastrar(Pacote pacote) {
		entityManager.persist(pacote);
	}
	
	private static Pacote buscarPorId(Pacote pacote) {
		Pacote pacotes = entityManager.find(Pacote.class, id);
	}
 */
}